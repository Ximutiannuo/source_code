-- ProjectControls 数据库初始化脚本

CREATE DATABASE IF NOT EXISTS projectcontrols CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE projectcontrols;

-- 注意：表结构将通过SQLAlchemy自动创建
-- 此文件用于手动创建数据库和初始配置

-- 创建索引（如果需要）
-- CREATE INDEX idx_activity_id ON activities(activity_id);
-- CREATE INDEX idx_activity_block ON activities(block);
-- CREATE INDEX idx_activity_discipline ON activities(discipline);
-- CREATE INDEX idx_activity_work_package ON activities(work_package);

